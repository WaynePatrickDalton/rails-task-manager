<h1>Pop in some Philisophy Form 🎯</h1>

<%= simple_form_for(@tasks) do |task| %>
  <%= task.label :title %>
  <%= task.text_field :title %>
  <%= task.label :details %>
  <%= task.text_field :details %>
  <%= task.label :completed %>
  <%= task.check_box :completed %>
  <%= task.submit %>
<% end %>